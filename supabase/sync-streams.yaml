config:
  edition: 3
streams:
  user_notes:
    auto_subscribe: true
    queries:
      - SELECT * FROM notes WHERE user_id = auth.user_id()
      - SELECT * FROM attachments WHERE user_id = auth.user_id()
